<%= render 'shared/navbar' %>

<h1>Thanks for your booking!</h1>
<div class="container">
    <div class="row">
        <div class="col-md-6">
            <p>Your vacation begins at:
                <%= @booking.starts_at%></p>
        </div>
        <div class="col-md-6">
            <p>Your vacation ends at:<%= @booking.ends_at %></p>
        </div>
    </div>
